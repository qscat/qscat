[project]
name = "qscat"
version = "0.4.2"
requires-python = ">=3.10"

[dependency-groups]
dev = [
  "ruff==0.14.2",
  "ty>=0.0.1a25",
]

[tool.ruff]
exclude = ["qscat/lib/xalglib.py"]

[tool.ty.rules]
unresolved-import = "ignore"

[tool.ty.src]
exclude = ["qscat/lib/xalglib.py"]